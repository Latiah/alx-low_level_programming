#include "main.h"
/**
 * add - a function that calculates sum
 * @a: first value
 * @b: second value
 * Return: returns the summation of numbers
 */
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
