#include "main.h"

/**
 * _islower  - a function that checks if a letter is in lowercase
 * @c: the characher
 * Return:  1 for true ,0 for false
 */
int _islower(int c)
{
c = 'a';
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
