#include "main.h"
#include<ctype.h>
/**
 * _isupper - for turning in upper case
 * @c: character that will be entered
 *Return: value to be returned
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
