#include "main.h"
#incude<stdlib.h>
/**
 *_atoi -a function that changes a string to a number
 * @s: string to be entered
 * Return: what is returned
 */
int _atoi(char *s)
{
atoi(s);
if s = ' ';
return (0);
}
