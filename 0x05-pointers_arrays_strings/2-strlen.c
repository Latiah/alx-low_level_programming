#include "main.h"
#include<string.h>
/**
 * _strlen -for giving us the length of a string
 * @s: string to be entered
 * Return: length of the string
 */
int _strlen(char *s)
{
int length;
length = strlen(*s);
return (length);
}
