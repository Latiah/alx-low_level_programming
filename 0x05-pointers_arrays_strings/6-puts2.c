#include<stdio.h>
#include "main.h"
#include<string.h>
/**
 * puts2 - afunction that displays some numbers
 * @str: string to be passed
 */
void puts2(char *str)
{
char s[];
int i = 0;
l = strlen(str);
*str = s[0];
while (i < l)
{
printf("%s\n", str);
i = i + 2;
}
}
