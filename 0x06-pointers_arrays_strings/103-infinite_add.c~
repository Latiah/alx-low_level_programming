#include "main.h"
/**
 * infinite_add -adding infinite numbers
 * @n1:number1
 * @n2:number2
 * @r:buffer
 * @size_r:buffer size
 * Return: return value
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
int sum ;
sum = n1 + n2;
return (sum);
}
