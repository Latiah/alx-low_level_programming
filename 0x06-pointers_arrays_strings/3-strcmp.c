#include<stdio.h>
#include "main.h"
/**
 * _strcmp -comparing strings
 * @s1: first string
 * @s2: second string
 * Return:value returned
 */
int _strcmp(char *s1, char *s2)
{
int i;
for (i = 0; s1[i] != '\0'; i++)
{
if (s1[i] != s2[i])
{
break;
}
if (s[i] == s2[i])
{
return (0);
}
}
}
