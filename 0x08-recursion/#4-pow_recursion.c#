#include "main.h"
#include<math.h>
#include<stdio.h>
/**
 * _pow_recursion -returns power
 * @x: base
 * @y: power
 * Return: result returned
 */
int _pow_recursion(int x, int y)
{
if (y < 0)
return (-1);
int power;
power = pow(x, y);
return (power);
}
