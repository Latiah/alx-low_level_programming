#include "main.h"
#include<string.h>
/**
 * _strlen_recursion -returns the lenght of a string
 * @s:string entered
 * Return: what is returned
 */
int _strlen_recursion(char *s)
{
int l;
l = strlen(s);
return (l);
}
