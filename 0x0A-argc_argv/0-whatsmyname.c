#include<stdio.h>
#include<stdlib.h>
/**
 * main -start of a program
 * @argc: argument count
 * @argv:argument vector
 * Return: 0 always a success
 */
int main(int argc, char *argv[])
{
if (argc == 1)
printf("%s\n", argv[0]);
return (0);
}
