#include "main.h"
#include<string.h>
#include<stdio.h>
#include<stdlib.h>
char *_strdup(const char *str)
{
char *dest = malloc(strlen(str) + 1);
if (dest == NULL)
return (NULL);
strcpy(dest, str);
return (dest); 
}
